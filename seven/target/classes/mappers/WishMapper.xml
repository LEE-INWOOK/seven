<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.seven.mapper.WishMapper">
	
<!-- 	<select id="getWishList" resultType="com.seven.domain.WishBean"> -->  <!--  wish 테이블의 정보만 가져옴 -> product관련 정보 없음 -->
<!-- 		select * from wish from member_id=#{id} order by wish_date desc -->
<!-- 	</select> -->
	
	<select id="getWishProductList" resultType="com.seven.domain.ProductBean">
		select * from product where product_num in (select product_num from wish where member_id=#{member_id} )
	</select>
	
	<!-- 추가 삭제 확인 상품상세페이지 및 리스트에서 사용 -->
	<select id="wishCheck" resultType="com.seven.domain.WishBean">
		select * from wish where member_id=#{member_id} and product_num=#{product_num}
	</select>

	<delete id="wishDelete"> <!-- 리턴값 없음  -->
		delete from wish where member_id=#{member_id} and product_num=#{product_num}
	</delete>
	
	<insert id="wishInsert"> <!-- 리턴값 없음  -->
		insert into wish (wish_date, product_num, member_id) values (#{wish_date}, #{product_num}, #{member_id})
	</insert>
	<!-- 추가 삭제 확인 상품상세페이지 및 리스트에서 사용 -->

</mapper>